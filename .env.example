VITE_API_URL=/api
# Producción (Vercel): URL ABSOLUTA de la API en Railway. Sin esto → Failed to fetch en el celu.
# VITE_API_URL=https://TU-API.up.railway.app/api
#
# Socket.io (flota / viajes en vivo).
# Opcional en local; por defecto usa el origin de Vite (proxy /socket.io → :3000).
# VITE_SOCKET_URL=http://localhost:3000
# En Vercel:
# VITE_SOCKET_URL=https://TU-API.up.railway.app
#
# Mapbox GL — opcional para mapas admin.
VITE_MAPBOX_TOKEN=
